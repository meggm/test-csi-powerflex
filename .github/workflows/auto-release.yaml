name: Auto Release CSI-Powerflex

on:
  workflow_dispatch:
  repository_dispatch:
    types: [auto-driver-module-release-workflow]

jobs:
  csm-release:
    uses: meggm/common-github-actions-test/.github/workflows/dummy-test.yaml@main
    with:
      secret_value: ${{ github.event.client_payload.gpg_key }}




